//LEVEL: MEDIUM

//see freshworks round 2 question in evernote -> interview questions

//given any object, create a object.get('key') method (callable on object) that returns the provided 
//key if present anywhere in the nested object, otherwise return undefined
//assuming there are no duplicates with same key name